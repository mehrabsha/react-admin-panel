version: '3.1'

services:
  stage-tosanex-admin-front:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "stage-tosanex-admin-front"
    restart: always
    ports:
    - 127.0.0.1:8085:3000